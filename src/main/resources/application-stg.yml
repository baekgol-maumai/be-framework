service:
  document:
    server-url:
      - https://be-framework-stg.maum.ai
  security:
    allowed-origins:
      - https://be-framework-stg.maum.ai
      - http://localhost:3000
      - http://localhost:1234
  db:
    uri: ${DB_URI}
    database: ${DB_DATABASE}